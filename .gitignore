.aider*

# Secrets — never commit real values, only .env.example
.env
.env.local
.env.*.local

# .NET build output (Pos.Api has no .gitignore of its own yet)
Pos.Api/bin/
Pos.Api/obj/

# Database backups — never commit customer data
backups/
